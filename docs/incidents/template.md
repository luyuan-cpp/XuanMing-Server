# [INC-YYYYMMDD-NNN][P0] 事故标题

> **状态**：调查中  
> **类型**：`crash` / `availability` / `data` / `security` / `split-brain` / `near-miss`  
> **环境**：生产 / 测试 / 本机 k8s / 本机进程  
> **首次发生时间（UTC）**：YYYY-MM-DD HH:MM:SS  
> **首次发现时间（UTC）**：YYYY-MM-DD HH:MM:SS  
> **负责人**：待指定  
> **受影响服务/版本**：service、commit、image digest  
> **最后更新**：YYYY-MM-DD

## 0. 一句话结论

用一到三句话写清直接根因、用户影响和当前状态。根因未知时明确写“仍在调查”，不得写未经证明的猜测。

## 1. 影响与范围

- 玩家影响：
- 影响人数/对局/请求数：
- 服务影响：
- 数据与安全影响：
- 开始/结束时间：
- 是否仍可复发：
- 严重级别判定理由：

## 2. 第一现场与证据

### 2.1 症状

- 客户端症状：
- 服务端症状：
- K8s/Agones 状态：

### 2.2 原始证据

记录日志平台查询条件、Pod、时间范围、trace/match/operation ID、退出码、堆栈和产物位置。禁止复制 JWT、DSTicket、密码、私钥、Secret 或 Authorization header。

```text
最小脱敏日志/堆栈
```

### 2.3 已排除的噪声

列出同时出现但经证据排除的报错，说明为什么不是本事故根因。

## 3. 时间线

以 UTC 为主；其他时区单独列出并注明换算。

| UTC 时间 | 组件 | 事件 | 证据 |
|---|---|---|---|
| HH:MM:SS.mmm | service | event | log/query |

## 4. 调用链与关键变量

```text
入口
  → middleware
  → service
  → biz
  → data / downstream
  → failure
```

| 变量/对象 | 创建位置 | 所有者与生命周期 | 是否共享/可变 | 事故中的作用 |
|---|---|---|---|---|
| `ctx` | | | | |

## 5. 根因

### 5.1 直接根因

写出最小必要条件和确定证据，区分事实与推断。

### 5.2 触发条件

- 

### 5.3 故障放大因素

- 

### 5.4 为什么现有保护没有挡住

分别说明 Recovery、重试、超时、租约、fencing、熔断、补偿、探针或多副本为何无效或不足。

## 6. 全仓同类问题扫描

- 扫描基线 commit：
- 扫描目录和文件类型：
- 搜索模式/工具：
- Confirmed 同型命中：
- 结构性隐患：
- 已排除项及理由：
- 未覆盖边界：

## 7. 处置与永久修复

### 7.1 临时止血

| 动作 | 状态 | 证据 | 风险/回滚 |
|---|---|---|---|

### 7.2 永久修复

| 项目 | 状态 | 代码/配置 | 验证 |
|---|---|---|---|

### 7.3 防复发规则

列出新增/修订的 `AGENTS.md`、`CLAUDE.md`、设计和运维规范链接。

## 8. 验证矩阵

| 验证 | 修复前结果 | 修复后结果 | 环境/命令 | 证据 |
|---|---|---|---|---|
| 针对性单测 | | | | |
| 集成回归 | | | | |
| `go test -race` | | | | |
| fatal/OOM/SIGKILL 重启注入 | | | | |
| 玩家 E2E | | | | |

未执行、失败或受阻的验证必须保留在表中，不能删除。

## 9. 部署、回滚与观察

- 修复 commit：
- 构建产物/镜像 digest：
- 部署时间与目标环境：
- 实际 Pod `imageID` / GameServer provenance：
- 回滚条件和步骤：
- 观察窗口、指标与结果：

## 10. 剩余风险与行动项

| ID | 严重级别 | 行动项 | 负责人 | 状态 | 目标/关联 Incident |
|---|---|---|---|---|---|

## 11. 关闭审核

- [ ] 直接根因和放大因素均有证据
- [ ] 修复前失败、修复后通过的回归存在
- [ ] race/集成/故障注入达到本事故风险要求
- [ ] 同类代码扫描完成
- [ ] 目标环境已加载可追溯的新产物
- [ ] 玩家路径、恢复和补偿路径验证通过
- [ ] 观察窗口无复发
- [ ] 剩余风险已解决或另建 Incident/任务
- [ ] 文档已脱敏且时间线时区明确

**关闭结论与审批人**：未关闭。

