package configtable

import (
	configpb "{{.PbImport}}"
)

// {{.Name}}.go — {{.GoName}}Table 手写伴生文件。
// 首次由 configtable-gen 创建(仅当文件不存在),此后归人维护,生成器不再覆盖。
// 表私有的逐行业务校验写在 validate{{.GoName}}Row;域方法(业务语义查询)也加在本文件。

// validate{{.GoName}}Row 逐行业务校验(生成的 new{{.GoName}}Table 调用;
// 主键非零/唯一已由生成代码兜住,类型/必填/枚举已由生成器在导表阶段校验,
// 这里只写服务端仍须 fail-closed 的业务约束;无约束保持 return nil)。
func validate{{.GoName}}Row(row *configpb.{{.RowType}}) error {
	_ = row
	return nil
}
