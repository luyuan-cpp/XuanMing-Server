-- 000003 回滚:有意 no-op。
--
-- idx_created 属于当前权威表定义(deploy/mysql-init fresh-init 建表自带;000002 保持
-- HEAD 原样不原地改,迁移路径统一由本 000003 条件补齐),up 只是给缺索引的库补缺口。
-- 回滚时删索引会让"fresh 建表 + 回滚"的库比权威定义少索引(审计 P1:回滚态与权威
-- 定义不一致),因此保留索引,不做任何操作。

SELECT 1;
