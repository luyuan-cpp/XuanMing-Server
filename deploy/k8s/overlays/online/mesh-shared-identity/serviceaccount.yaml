# battle-result 同时调用 Inventory 与 DSAllocator terminal RPC；由共享静态 component
# 唯一拥有 ServiceAccount，避免两个候选组合时重复资源。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pandora-battle-result
  namespace: pandora
automountServiceAccountToken: false
