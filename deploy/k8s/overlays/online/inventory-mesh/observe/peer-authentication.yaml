apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: pandora-inventory-mtls
  namespace: pandora
  annotations:
    pandora.dev/migration-phase: observe
spec:
  selector:
    matchLabels:
      app: inventory
  mtls:
    mode: PERMISSIVE
